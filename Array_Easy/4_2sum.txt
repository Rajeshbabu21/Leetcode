class Solution {
    public int[] twoSum(int[] nums, int target) {
        int n = nums.length;

        HashMap<Integer,Integer> ans = new HashMaap<>();

        for(int i=0;i<n;i++)
        {
            int compliment = target - nums[i];

            if(ans.containsKey(compliment))
            {
            return new int[]{ans.get(compliment),i};
            }
            map.put(nums[i],i);

        }
        return new int[]{-1,-1};
        
    }
}