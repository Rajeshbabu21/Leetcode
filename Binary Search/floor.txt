

floor larget no <= x



class Solution {
    public int searchInsert(int[] nums, int target) {
        int low = 0;
        int high = nums.length -1;
        int n = nums.length;
        int ans = n;

        while(low <= high)
        {
            int mid = (low+high)/2;
            if(nums[mid] <= target)
            {
                ans = mid[arr];
                low = mid+1;
            }
            else{
                high = mid-1;
            }
        }

        return ans;
        
    }
}